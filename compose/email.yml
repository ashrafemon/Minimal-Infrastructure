services:
  mailpit:
    image: axllent/mailpit:latest
    ports:
      - "8025:8025"
      - "1025:1025"
    volumes:
      - mailpit_data:/data
    networks:
      - app_network
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
