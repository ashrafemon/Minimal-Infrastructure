services:
  nginx-proxy-manager:
    image: jc21/nginx-proxy-manager:latest
    ports:
      - "80:80"
      - "443:443"
      - "81:81"
    environment:
      DB_SQLITE_FILE: /data/database.sqlite
    volumes:
      - npm_data:/data
      - ./nginx/ssl:/etc/letsencrypt
    networks:
      - app_network
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
